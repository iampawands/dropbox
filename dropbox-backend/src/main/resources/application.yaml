spring:
  application:
    name: dropbox
  datasource:
    url: jdbc:mysql://localhost:3306/dropbox
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


